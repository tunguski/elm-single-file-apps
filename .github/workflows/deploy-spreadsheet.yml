name: Build & deploy the Spreadsheet backend (spreadsheet.matsuo.pl)

# Builds elm.jar, compiles the spreadsheet single-file app, bundles servers/spreadsheet/SpreadsheetServer.elm into a standalone
# jar (reads DB_URL for H2, STATIC_DIR to serve the app UI), publishes a container image to GHCR, and
# redeploys it on the VPS behind nginx-proxy. Same pattern as the sudoku deploy.

on:
  push:
    branches: [master, main]
    paths:
      - "servers/spreadsheet/**"
      - "src/**"
      - "shared/**"
      - ".github/workflows/deploy-spreadsheet.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Check out elm-single-file-apps
        uses: actions/checkout@v4
      - name: Check out the elm-lang compiler
        uses: actions/checkout@v4
        with:
          repository: tunguski/elm-lang
          ref: master
          path: elm-lang
      - name: Set up GraalVM for JDK 25
        uses: graalvm/setup-graalvm@v1
        with:
          java-version: "25"
          distribution: "graalvm"
          cache: "maven"
          cache-dependency-path: elm-lang/pom.xml
      - name: Build the elm compiler (elm.jar)
        working-directory: elm-lang
        run: |
          chmod +x ./mvnw
          ./mvnw -B -ntp clean package -DskipTests
      - name: Build the spreadsheet app + bundle the server
        run: |
          chmod +x ./build.sh
          ELM="java -jar $GITHUB_WORKSPACE/elm-lang/target/elm.jar" ./build.sh spreadsheet
          cp build/spreadsheet.html servers/spreadsheet/index.html
          java -jar elm-lang/target/elm.jar bundle server             "$PWD/servers/spreadsheet/SpreadsheetServer.elm" --port 8080 -o servers/spreadsheet/server
          ls -la servers/spreadsheet/server.jar servers/spreadsheet/index.html
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Derive lowercase image name
        id: img
        run: echo "name=ghcr.io/${GITHUB_REPOSITORY,,}/spreadsheet-server" >> "$GITHUB_OUTPUT"
      - name: Build and push the image
        uses: docker/build-push-action@v5
        with:
          context: servers/spreadsheet
          file: servers/spreadsheet/Dockerfile
          push: true
          tags: |
            ${{ steps.img.outputs.name }}:latest
            ${{ steps.img.outputs.name }}:${{ github.sha }}
          cache-from: type=gha,scope=spreadsheet-server
          cache-to: type=gha,scope=spreadsheet-server,mode=max

  deploy:
    needs: build-and-push
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Check out (for the compose file)
        uses: actions/checkout@v4
      - name: Encode the compose file for transport
        run: echo "COMPOSE_B64=$(base64 -w0 servers/spreadsheet/docker-compose.yml)" >> "$GITHUB_ENV"
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: vps-7d690b64.vps.ovh.net
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          envs: COMPOSE_B64
          script: |
            set -e
            echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io -u ${{ github.actor }} --password-stdin
            mkdir -p ~/containers/elm-single-file-apps/spreadsheet-server
            echo "$COMPOSE_B64" | base64 -d > ~/containers/elm-single-file-apps/spreadsheet-server/docker-compose.yml
            podman rm -f spreadsheet 2>/dev/null || true
            cd ~/containers/elm-single-file-apps/spreadsheet-server
            podman pull ghcr.io/tunguski/elm-single-file-apps/spreadsheet-server:latest
            # Pin the project name so the H2 named volume (spreadsheet-server_spreadsheet-db) is stable regardless of the compose folder.
            podman compose -p spreadsheet-server up -d --force-recreate
